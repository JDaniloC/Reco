build:
  docker:
    web: python_backend/Dockerfile
  whatsapp: yarn --cwd ./nestjs_server build
  
run:
  web: uvicorn main:app --host 0.0.0.0 --port $PORT
  whatsapp: yarn --cwd ./nestjs_server start:prod